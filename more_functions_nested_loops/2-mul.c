#include <stdio.h>
/**
 * mul - Multiplica 2 integradores
 * @a: el primer integrador
 * @b: el segundo inegrador
 *
 * Return: el prod de 2 integradores
 */
int mul(int a, int b)
{
return (a * b);
}
