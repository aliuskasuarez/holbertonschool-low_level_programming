#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/* more headers goes there */
/* betty style doc for function main goes there */
/**
 * main - asasaradap 
 *
 *  Return: Always 0 (Success)
 */
int main()void)
{
	int main () {
	srand(time(0));
	int n = rand();
	int last_digit = n % 10; 
	n = rand() - RAND_MAX / 2;
		return (0);
}
