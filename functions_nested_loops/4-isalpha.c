#include <stdio.h>
/**
 * int _islower - islower
 *
 * Description:  a function islower
 *
 */
int _isalpha(int c)
{
if ((c >= 'A' && c <= 'Z') || (c >= 'a' && c <= 'z'))
{
return (1);
}
return (0);
}
